
<br><br><br>
<%= form_with(model: @comment, local: true) do |form| %>
  <div class="field">
    <%= form.text_area :content, class: "comment_input" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
